# Status page module (Keprix)

Public status lives at `/status` and `/api/status/public`.

Hard rules: separate marketing presentation from API outages when possible; auto-create after 3 failed checks; 48h maintenance lead time.
