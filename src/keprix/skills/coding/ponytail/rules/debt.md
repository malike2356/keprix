# ponytail-debt

Harvest every `ponytail:` comment into a debt ledger. One row per marker:

`<file>:<line>, <what was simplified>. ceiling: <limit>. upgrade: <trigger>.`

Tag entries missing a trigger as `no-trigger`.
