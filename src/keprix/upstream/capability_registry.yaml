# Keprix capability registry for Hermes upstream equivalence matching.
# Keys are stable capability ids; values are searchable descriptions and aliases.

capabilities:
  agent-runtime: "conversation loop agent engine tool calling streaming sessions"
  provider-routing: "smart routing combos quota auto-fallback model providers openai anthropic gemini"
  compression: "RTK caveman token compression context PII masking injection defense"
  memory-system: "workspace session memory persist colleague continuity gbrain"
  tools-mcp: "MCP tools browser execute shell toolsets tool policy"
  credential-vault: "credential vault API keys proxy rotation secrets"
  prompt-cache: "prompt cache spend tracker format translator headroom"
  tui-command-center: "textual TUI CLI command center dashboard slash commands"
  channel-shield: "channel shield inbound email messaging quarantine"
  scout-security: "scout signals governance sandbox hardening prompt guard"
  billing-stripe: "SaaS billing Stripe subscriptions quotas"
  upgrade-system: "upgrade check plan execute alerts scheduler rollback"
  a2a-protocol: "agent-to-agent A2A protocol compliance observability"
  playbooks: "playbooks workflows visual YAML runs approvals"
  agent-apps: "agent apps marketplace manifests webhooks"
  mutation-engine: "mutation engine self-improvement tools skills"
  cron-jobs: "cron scheduler jobs terminal file toolsets"
  mobile-client: "Android mobile client companion"
  workspace-ui: "Next.js workspace chat documents tasks settings"
  upstream-monitor: "Hermes upstream release tracking adoption pipeline"

# Maps Hermes-oriented keywords to Keprix capability ids (boosts matching).
aliases:
  browser: tools-mcp
  mcp: tools-mcp
  tool: tools-mcp
  provider: provider-routing
  model: provider-routing
  routing: provider-routing
  fallback: provider-routing
  compress: compression
  token: compression
  context: compression
  memory: memory-system
  session: memory-system
  workspace: memory-system
  cache: prompt-cache
  tui: tui-command-center
  cli: tui-command-center
  dashboard: tui-command-center
  sandbox: scout-security
  security: scout-security
  billing: billing-stripe
  stripe: billing-stripe
  upgrade: upgrade-system
  cron: cron-jobs
  android: mobile-client
  playbook: playbooks
  a2a: a2a-protocol
