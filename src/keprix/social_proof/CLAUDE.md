# Social proof (Keprix)

Never fabricate quotes. Every item needs a source URL.
Human approval required before `/proof` publication.
Weekly collect via `run_weekly` / `POST /api/social-proof/collect?weekly=true`.
