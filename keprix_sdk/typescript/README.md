# Keprix App Foundation SDK (TypeScript)

```bash
npm install
npm run build
```

```typescript
import { KeprixApp, Domain, Entity, Field, Operation } from "@keprix-ai/sdk";
```

See `examples/invoice-app.ts` and `examples/crm-app.ts`.
