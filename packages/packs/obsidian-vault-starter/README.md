# Obsidian Vault Starter

This Hub pack advertises the Obsidian-compatible vault starter.

Initialize a vault with:

```bash
keprix vault init --pack obsidian-starter --path /path/to/vault
```

The source files live in `src/keprix/optional-skills/productivity/obsidian-vault-starter/`.
