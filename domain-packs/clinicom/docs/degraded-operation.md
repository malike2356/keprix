# Degraded operation
When a provider is unavailable, capability discovery must report `stub` or `disabled`. Continuity output is explicitly labelled, preserves available text, and requires human review. Do not silently substitute a model, claim live inference, or change the Contabo sidecar profile.
