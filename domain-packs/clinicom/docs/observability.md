# Observability
Log correlation IDs, capability name, status, source label, latency, errors, safety escalation count, and fallback count.

Never log raw utterances, audio, patient names, NHS numbers, addresses, full clinical history, bootstrap credentials, or service tokens. Metrics must remain aggregate and organisation-safe.
