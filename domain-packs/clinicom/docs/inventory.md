# Inventory
- Keprix pack: `http_app.py`, `tools/`, contract 2.0 routes, and `/v1/products/clinicom/*`.
- Carina pack: `carina/02-backends/core.carinaai.uk/packages/packs/clinicom/`; live Contabo profile today.
- Local clone: `clinicom-ai/sidecar/`; development continuity only.
- Clinicom product APIs: `/api/health`, `/api/capabilities`, and `/api/keprix/v1`.
- Contabo profile switch: `clinicom-ai/deploy/contabo-temp/switch-sidecar.sh`.
