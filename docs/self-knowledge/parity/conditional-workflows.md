# Conditional workflows

Condition ops: eq, ne, present, in.
Template vical_confirmed_create_lead runs on BookingLifecycle side effects when status is confirmed.
Dry-run helper remains for tests and docs.
