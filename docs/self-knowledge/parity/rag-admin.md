# RAG admin

Operator UI: /data?tab=rag (RagPipelinePanel -> /api/rag-pipeline).
Facade: GET/POST /api/rag-admin/pipelines|ingest uses real RagPipeline.ingest.
Training jobs are unsupported in CE (honest status on the admin payload).
