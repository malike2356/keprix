# Product leads

Thin lead store (not a CRM). Tools: create_lead, list_leads, link_booking_to_lead.
On viCal booking confirmed, execute_booking_confirmed_workflow creates and links a lead
when KEPRIX_VICAL_LEAD_WORKFLOW is on (default).
