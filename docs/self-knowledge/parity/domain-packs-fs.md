# Domain packs on disk

First-party packs under domain-packs/:
- research-intel (brief checklist tool)
- scheduling-ops (ensure host / viCal seed)
- clinicom

Listed by GET /api/domain-packs via filesystem discovery.
