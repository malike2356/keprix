# Environment variables

<!-- AUTO-GENERATED by scripts/generate_doc_pages.py; do not edit by hand. -->


Parsed from `.env.example` at build time.

| Variable | Description | Default | Required |
| --- | --- | --- | --- |
| `KEPRIX_VERSION` | Keprix - Environment Configuration All Keprix-specific variables use the KEPRIX_ prefix (uppercase). Provider API keys use their canonical upstream names so the same .env works with local model servers without modification. Product | `0.16.0` | no |
| `KEPRIX_DEVELOPER_MODE` | Developer Mode Set automatically by 'keprix init' when you confirm you are the owner. Do not set manually. | `false` | no |
| `KEPRIX_DEFAULT_PROVIDER` | LLM Providers | `auto` | no |
| `KEPRIX_CODEBASE_AWARENESS` | Web chat: inject read-only codebase map + docs into the system prompt (true/false). | `true` | no |
| `KEPRIX_SELF_KNOWLEDGE_BOOTSTRAP` | Shared RAG corpus so Keprix can answer about its own capabilities/codebase. Indexed under user_id __keprix_self__ (override with KEPRIX_SELF_KNOWLEDGE_USER_ID). | `true` | no |
| `KEPRIX_AUTO_MCP_SPAWN` | Optional: KEPRIX_SELF_KNOWLEDGE_MAX_FILES (default 1500) Optional: KEPRIX_CODEBASE_ROOT Optional: KEPRIX_REPO_ROOT when the API runs outside the install directory Allow the agent to spawn MCP servers from the built-in catalog during tasks. When unset, falls back to mcp.auto_spawn_enabled in config.yaml (toggle via /admin/mcp). | `false` | no |
| `OPENAI_API_KEY` | General | `(empty)` | yes |
| `ANTHROPIC_API_KEY` | General | `(empty)` | yes |
| `DEEPSEEK_API_KEY` | General | `(empty)` | yes |
| `GROQ_API_KEY` | General | `(empty)` | yes |
| `OPENROUTER_API_KEY` | General | `(empty)` | yes |
| `TOGETHER_API_KEY` | General | `(empty)` | yes |
| `MISTRAL_API_KEY` | General | `(empty)` | yes |
| `GEMINI_API_KEY` | General | `(empty)` | yes |
| `GOOGLE_API_KEY` | General | `(empty)` | yes |
| `OLLAMA_BASE_URL` | General | `http://host.docker.internal:11434/v1` | no |
| `LM_STUDIO_URL` | General | `http://host.docker.internal:1234/v1` | no |
| `CUSTOM_LLM_BASE_URL` | General | `(empty)` | yes |
| `CUSTOM_LLM_API_KEY` | General | `(empty)` | yes |
| `KEPRIX_DATABASE_URL` | Database REQUIRED: set strong passwords before docker compose up (no changeme in production). bash scripts/generate-production-env.sh | `postgresql+asyncpg://keprix:REPLACE_ME_POSTGRES@localhost:5432/keprix` | no |
| `POSTGRES_PASSWORD` | General | `REPLACE_ME_POSTGRES` | no |
| `POSTGRES_BIND` | General | `127.0.0.1` | no |
| `POSTGRES_HOST_PORT` | General | `5432` | no |
| `KEPRIX_REDIS_URL` | Redis Cache | `redis://:REPLACE_ME_REDIS@localhost:6379` | no |
| `REDIS_PASSWORD` | General | `REPLACE_ME_REDIS` | no |
| `REDIS_BIND` | General | `127.0.0.1` | no |
| `REDIS_HOST_PORT` | General | `6379` | no |
| `SEARXNG_BIND` | Web Search (SearXNG - self-hosted) Start SearXNG: docker compose -f docker-compose.searxng.yml up -d Then set SEARXNG_URL in Settings -> Web search (or here for non-UI setups). | `127.0.0.1` | no |
| `SEARXNG_PORT` | General | `8080` | no |
| `SEARXNG_URL` | General | `http://localhost:8080` | no |
| `KEPRIX_SEARXNG_URL` | General | `http://localhost:8080` | no |
| `SEARXNG_SECRET` | General | `(empty)` | yes |
| `BACKEND_BIND` | Server Binding | `127.0.0.1` | no |
| `BACKEND_PORT` | General | `3333` | no |
| `FRONTEND_BIND` | General | `127.0.0.1` | no |
| `FRONTEND_PORT` | General | `3000` | no |
| `AUTH_ENABLED` | Auth and Sessions Auth on by default. Set false only for locked-down local/dev experiments. | `true` | no |
| `KEPRIX_MULTI_USER` | Multi-user off by default (single-operator). Set true to allow additional accounts. | `false` | no |
| `KEPRIX_JWT_SECRET` | bash scripts/generate-production-env.sh --domain https://app.example.com Do not leave GENERATE_RANDOM_* or changeme values on a public host. | `GENERATE_RANDOM_64_CHARS_HERE` | no |
| `KEPRIX_SESSION_SECRET` | REQUIRED for production. Generate with | `GENERATE_RANDOM_64_CHARS_HERE` | no |
| `KEPRIX_TOTP_ISSUER` | REQUIRED for production. Generate with | `Keprix` | no |
| `KEPRIX_ALLOWED_ORIGINS` | Comma-separated browser origins (include https://your.domain) | `http://localhost:3000` | no |
| `KEPRIX_SESSION_TTL_DAYS` | REQUIRED for production. Generate with | `7` | no |
| `KEPRIX_HANDOFF_SECRET` | Shared HMAC secret for Carina Agent Studio -> Keprix Visual Playbook Studio handoff JWTs | `GENERATE_RANDOM_32_CHARS_HERE` | no |
| `KEPRIX_REQUIRE_2FA` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_IP_HASH_SALT` | Set automatically by keprix init if not present | `(empty)` | yes |
| `KEPRIX_CSP_EXTRA` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_REDACT_PRIVATE_IPS` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_AUDIT_FAIL_ON_HIGH` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_SHOW_ADMIN_PASSWORD` | Admin bootstrap (set before first boot; read from .env only). Optional KEPRIX_ADMIN_EMAIL (example: admin@example.com) Optional KEPRIX_ADMIN_PASSWORD (change before first boot on shared hosts) Never print admin password on shared hosts (install.sh honors this; default off) | `0` | no |
| `KEPRIX_TRUSTED_PROXIES` | Immediate peer CIDRs allowed to supply X-Forwarded-For / X-Forwarded-Proto (Caddy/nginx on the same host: 127.0.0.1,::1; Cloudflare: use tunnel/origin IPs) | `127.0.0.1,::1` | no |
| `KEPRIX_LOG_LEVEL` | REQUIRED for production. Generate with | `info` | no |
| `KEPRIX_SMTP_HOST` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_SMTP_PORT` | REQUIRED for production. Generate with | `587` | no |
| `KEPRIX_SMTP_USER` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_SMTP_PASS` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_EMAIL_FROM` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_RESEND_API_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_CHROMADB_HOST` | Memory and RAG | `localhost` | no |
| `KEPRIX_CHROMADB_PORT` | REQUIRED for production. Generate with | `8100` | no |
| `KEPRIX_EMBEDDING_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_EMBEDDING_API_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_EMBEDDING_MODEL` | REQUIRED for production. Generate with | `text-embedding-3-small` | no |
| `KEPRIX_FASTEMBED_MODEL` | REQUIRED for production. Generate with | `sentence-transformers/all-MiniLM-L6-v2` | no |
| `KEPRIX_FASTEMBED_CACHE_PATH` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_NOTION_TOKEN` | Notion integration token for RAG ingestion (optional if using MCP OAuth only) | `(empty)` | yes |
| `TELEGRAM_BOT_TOKEN` | Messaging Channels | `(empty)` | yes |
| `TELEGRAM_WEBHOOK_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `DISCORD_BOT_TOKEN` | REQUIRED for production. Generate with | `(empty)` | yes |
| `DISCORD_APPLICATION_ID` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GOOGLE_CLIENT_ID` | Calendar | `(empty)` | yes |
| `KEPRIX_GOOGLE_CLIENT_SECRET` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GOOGLE_REDIRECT_URI` | REQUIRED for production. Generate with | `http://localhost:3000/oauth/google/callback` | no |
| `KEPRIX_GWS_ENABLED` | Google Workspace connector (OAuth desktop app, token stored outside repo) | `0` | no |
| `GOOGLE_WORKSPACE_CREDENTIALS_PATH` | REQUIRED for production. Generate with | `(empty)` | yes |
| `GOOGLE_WORKSPACE_TOKEN_PATH` | REQUIRED for production. Generate with | `(empty)` | yes |
| `GOOGLE_WORKSPACE_BRIDGE_CMD` | REQUIRED for production. Generate with | `(empty)` | yes |
| `GOOGLE_WORKSPACE_USE_GWS_CLI` | REQUIRED for production. Generate with | `0` | no |
| `GOOGLE_WORKSPACE_SERVICE_ACCOUNT_MODE` | REQUIRED for production. Generate with | `0` | no |
| `KEPRIX_GITHUB_CLIENT_ID` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GITHUB_CLIENT_SECRET` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_OIDC_CLIENT_ID` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_OIDC_CLIENT_SECRET` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_OIDC_ISSUER` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_OIDC_NAME` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_SSO_REDIRECT_URI` | REQUIRED for production. Generate with | `http://localhost:3333/api/auth/sso/callback` | no |
| `KEPRIX_FRONTEND_URL` | REQUIRED for production. Generate with | `http://127.0.0.1:3000` | no |
| `KEPRIX_CALDAV_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_CALDAV_USERNAME` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_CALDAV_PASSWORD` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_VAULT_KEY` | Vault Generate: openssl rand -base64 32 (or use scripts/generate-production-env.sh) | `GENERATE_RANDOM_32_CHARS_HERE` | no |
| `KEPRIX_MCP_ALLOWED_SERVERS` | MCP | `(empty)` | yes |
| `GRAPHITI_MCP_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GRAPHITI_ENABLED` | REQUIRED for production. Generate with | `1` | no |
| `KEPRIX_GOVERNANCE_ENABLED` | Governance (optional; disabled by default) | `false` | no |
| `KEPRIX_GOVERNANCE_PROVIDER` | REQUIRED for production. Generate with | `native` | no |
| `KEPRIX_GOVERNANCE_API_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GOVERNANCE_WORKSPACE_ID` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GOVERNANCE_ENDPOINT` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_GOVERNANCE_TRACE_EXPORT` | REQUIRED for production. Generate with | `false` | no |
| `SCOUT_ENABLED` | Scout integration (optional; uses governance endpoint/key when unset) | `false` | no |
| `SCOUT_API_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `SCOUT_ENDPOINT` | REQUIRED for production. Generate with | `https://console.labyrinthscout.com` | no |
| `SCOUT_REDIS_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_SCOUT_PRODUCT` | REQUIRED for production. Generate with | `keprix` | no |
| `CHANNEL_SHIELD_ENABLED` | Channel Shield (shared inbound protection; optional) | `false` | no |
| `CHANNEL_SHIELD_SMTP_HOST` | REQUIRED for production. Generate with | `0.0.0.0` | no |
| `CHANNEL_SHIELD_SMTP_PORT` | REQUIRED for production. Generate with | `2525` | no |
| `CHANNEL_SHIELD_CLAMAV_SOCKET` | REQUIRED for production. Generate with | `(empty)` | yes |
| `CHANNEL_SHIELD_YARA_RULES_DIR` | REQUIRED for production. Generate with | `(empty)` | yes |
| `CHANNEL_SHIELD_WEB_ORIGINS` | REQUIRED for production. Generate with | `(empty)` | yes |
| `CHANNEL_SHIELD_WEB_EMBED_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_ACTIVE_EXTENSIONS` | memory \| durable (default). Durable uses Postgres when KEPRIX_DATABASE_URL is set, else SQLite. Optional: CHANNEL_SHIELD_STORE (memory or durable) Optional: CHANNEL_SHIELD_SQLITE_PATH for durable SQLite fallback Comma-separated product extensions (e.g. scout) | `(empty)` | yes |
| `KEPRIX_ENABLED_PRODUCTS` | Comma-separated products built on Keprix (see config/products.example.yaml) | `(empty)` | yes |
| `KEPRIX_GENERATED_TOOLS_DIR` | Optional: KEPRIX_PRODUCTS_CONFIG path to products.yaml Mutation Engine | `/data/keprix/generated/tools` | no |
| `KEPRIX_GENERATED_SKILLS_DIR` | REQUIRED for production. Generate with | `/data/keprix/generated/skills` | no |
| `KEPRIX_MUTATION_ENABLED` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_SANDBOX_TIMEOUT` | REQUIRED for production. Generate with | `30` | no |
| `KEPRIX_GAP_CONFIDENCE` | REQUIRED for production. Generate with | `0.7` | no |
| `KEPRIX_MUTATION_ADMIN_CHANNEL` | REQUIRED for production. Generate with | `web` | no |
| `KEPRIX_MUTATION_MAX_RETRIES` | REQUIRED for production. Generate with | `2` | no |
| `KEPRIX_MUTATION_REQUIRED_CHANNELS` | REQUIRED for production. Generate with | `web_ui,telegram` | no |
| `KEPRIX_TOOL_SIGNING_KEY` | REQUIRED for production. Generate with | `/data/keprix/secrets/tool-signing-key.pem` | no |
| `KEPRIX_TOOL_VERIFY_KEY` | REQUIRED for production. Generate with | `/data/keprix/secrets/tool-verify-key.pem` | no |
| `KEPRIX_MUTATION_REQUIRE_APPROVAL` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_MUTATION_RATE_LIMIT` | REQUIRED for production. Generate with | `10` | no |
| `KEPRIX_MUTATION_TOOL_SYNTHESIS` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_MUTATION_PROMPT_EVOLUTION` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_MUTATION_SELF_CODING` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_MUTATION_BRANCH_PREFIX` | REQUIRED for production. Generate with | `mutation/` | no |
| `KEPRIX_MUTATION_MERGE_STRATEGY` | REQUIRED for production. Generate with | `squash` | no |
| `KEPRIX_MUTATION_REPO_ROOT` | REQUIRED for production. Generate with | `.` | no |
| `KEPRIX_MUTATION_AUTO_APPROVE_THRESHOLD` | REQUIRED for production. Generate with | `0.85` | no |
| `KEPRIX_MUTATION_SYNTHESIS_MIN_CONFIDENCE` | REQUIRED for production. Generate with | `0.75` | no |
| `KEPRIX_MUTATION_REQUIRE_TESTS` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_MUTATION_RETENTION_DAYS` | REQUIRED for production. Generate with | `365` | no |
| `KEPRIX_MUTATION_MAX_GENERATED_TOOLS` | REQUIRED for production. Generate with | `200` | no |
| `KEPRIX_MUTATION_PRUNE_AFTER_DAYS` | REQUIRED for production. Generate with | `90` | no |
| `KEPRIX_LOG_LEVEL` | Observability | `info` | no |
| `KEPRIX_TELEMETRY` | Anonymous usage telemetry (default off). Never sends conversation content or secrets. | `false` | no |
| `KEPRIX_BILLING_PROVIDER` | Optional: KEPRIX_OTLP_ENDPOINT (example http://localhost:4317) Billing (SaaS monetization) Provider: stripe (requires STRIPE_SECRET_KEY) or mock mode (KEPRIX_BILLING_ENABLED true) | `(empty)` | yes |
| `KEPRIX_BILLING_ENABLED` | REQUIRED for production. Generate with | `false` | no |
| `KEPRIX_BILLING_USE_EXAMPLE` | Path to product billing.yaml (default: active extension or billing.example.yaml when use-example is true) Optional: KEPRIX_BILLING_CONFIG path to billing.yaml | `false` | no |
| `KEPRIX_BILLING_CURRENCY` | REQUIRED for production. Generate with | `gbp` | no |
| `KEPRIX_BILLING_TAX_DEFAULT` | REQUIRED for production. Generate with | `0.20` | no |
| `KEPRIX_BILLING_ALLOW_UNSIGNED_WEBHOOKS` | REQUIRED for production. Generate with | `false` | no |
| `STRIPE_SECRET_KEY` | REQUIRED for production. Generate with | `(empty)` | yes |
| `STRIPE_WEBHOOK_SECRET` | REQUIRED for production. Generate with | `(empty)` | yes |
| `STRIPE_TEST_MODE` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_DATA_DIR` | Data Directories | `/data/keprix` | no |
| `KEPRIX_LOGS_DIR` | REQUIRED for production. Generate with | `/data/keprix/logs` | no |
| `KEPRIX_LLM_USAGE_ENABLED` | LLM usage and cost tracking (Prompt 145) | `true` | no |
| `KEPRIX_LLM_USAGE_RETENTION_DAYS` | REQUIRED for production. Generate with | `90` | no |
| `KEPRIX_LLM_USAGE_SQLITE_FALLBACK` | REQUIRED for production. Generate with | `true` | no |
| `KEPRIX_CHAT_GATEWAY_STREAM` | Upload Limits (bytes) Route workspace /chat through the WEB_UI gateway stream (mutation + tools + LLM). | `true` | no |
| `KEPRIX_CHAT_MUTATION_SIDECAR` | Optional: run the full agent tool loop in web chat (requires a configured LLM provider). When unset, developer/admin/owner installs default to true; set false to force LLM-only chat. Optional: KEPRIX_WEB_UI_AGENT_LOOP (false forces LLM-only chat) Legacy sidecar gap bridge (Prompt 139). Default off; agent loop hook handles mutation. | `false` | no |
| `KEPRIX_MUTATION_APPROVAL_TIMEOUT` | Seconds to wait for mutation approval before ending a paused agent turn. | `3600` | no |
| `KEPRIX_MUTATION_STREAM_WAIT_APPROVAL` | Keep /chat NDJSON stream open until mutation approve/reject (Prompt 28 parity). | `true` | no |
| `KEPRIX_MUTATION_LLM_TRIGGER` | Optional LLM-only mutation trigger in addition to tool_miss (default off). | `false` | no |
| `KEPRIX_CHAT_UPLOAD_MAX_BYTES` | REQUIRED for production. Generate with | `10485760` | no |
| `KEPRIX_DOCUMENT_UPLOAD_MAX_BYTES` | REQUIRED for production. Generate with | `26214400` | no |
| `KEPRIX_AUDIO_UPLOAD_MAX_BYTES` | REQUIRED for production. Generate with | `26214400` | no |
| `KEPRIX_NOTEBOOK_RESEARCH_ENABLED` | Notebook research bridge (optional external sidecar; Quick Notebook is native) | `true` | no |
| `NOTEBOOKLM_BRIDGE_CMD` | REQUIRED for production. Generate with | `(empty)` | yes |
| `NOTEBOOKLM_MCP_URL` | REQUIRED for production. Generate with | `(empty)` | yes |
| `KEPRIX_DESIGN_PREVIEW_ENABLED` | Design live preview | `true` | no |
| `KEPRIX_INSTANCE_URL` | Optional: KEPRIX_WORKSPACE_ROOT path Agent Apps (manifest-driven portable apps; see docs/features/agent-apps.md) Optional: KEPRIX_AGENT_APPS_DIR (default ~/.keprix/agent-apps) Optional: KEPRIX_AGENT_APPS_CONFIG (default config/agent_apps.yaml) Optional: KEPRIX_AGENT_APP_RUN_RETENTION_DAYS (default 30) Optional: KEPRIX_AGENT_APPS_ENABLED (default true) GPU Support (Docker Compose overrides) NVIDIA: set COMPOSE_FILE to docker/docker-compose.yml:docker/gpu-nvidia.yml AMD: set COMPOSE_FILE to docker/docker-compose.yml:docker/gpu-amd.yml Public HTTPS origin (Stripe returns, links, CORS companion to KEPRIX_ALLOWED_ORIGINS) | `http://localhost:3000` | no |
