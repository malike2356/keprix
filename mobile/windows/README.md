# Windows companion shell (Prompt 25)

Minimal placeholder for a native Windows hub client. Connects to a self-hosted
keprix backend using the same companion pairing and REST API contract as iOS and
Android.

See `mobile/ios` and `mobile/android` for the full native implementations.
