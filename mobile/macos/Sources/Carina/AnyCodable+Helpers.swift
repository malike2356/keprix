import Foundation
import CarinaKit

// Prefer the CarinaKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = CarinaKit.AnyCodable
typealias InstanceIdentity = CarinaKit.InstanceIdentity
