# Express Universal Sidecar starter

```bash
export DEMO_TOKEN=replace-with-local-demo-token
export KEPRIX_SIDECAR_URL=http://127.0.0.1:3360
export KEPRIX_PROJECT_KEY=express_demo
npm install
npm start
```

Listens on `127.0.0.1:8098`. See `keprix.sidecar.yaml`.
